import {Button} from '../ui/Button';import {formatCurrency} from '../../lib/formatters';import type {SaleWithItem} from '../../hooks/useSales';import {PaymentStatusBadge} from './PaymentStatusBadge';import {SaleStatusBadge} from './SaleStatusBadge';import {Ban,Coins,Pencil} from 'lucide-react'
export function SaleCard({sale,clientName,onEdit,onPayment,onCancel}:{sale:SaleWithItem;clientName:string;onEdit:()=>void;onPayment:()=>void;onCancel:()=>void}){return <article className="rounded-2xl border border-border bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex flex-wrap gap-2"><PaymentStatusBadge status={sale.payment_status}/><SaleStatusBadge status={sale.delivery_status}/></div><h3 className="mt-3 font-bold">{sale.sale_code||'Venda'}</h3><p className="mt-1 text-sm text-slate-500">{clientName} · {sale.item?.product_name}</p></div><strong className="text-lg text-primary">{formatCurrency(sale.total_value)}</strong></div><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><span><small className="block text-slate-500">Pago</small>{formatCurrency(sale.paid_value)}</span><span><small className="block text-slate-500">Em aberto</small>{formatCurrency(sale.open_value)}</span></div>{sale.payment_status!=='cancelado'&&<div className="mt-4 flex flex-wrap gap-2"><Button variant="outline" className="min-h-9 px-3" onClick={onEdit}><Pencil size={16}/>Editar</Button>{sale.open_value>0&&<Button variant="outline" className="min-h-9 px-3" onClick={onPayment}><Coins size={16}/>Pagamento</Button>}<Button variant="ghost" className="min-h-9 px-3 text-danger" onClick={onCancel}><Ban size={16}/>Cancelar</Button></div>}</article>}
